<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="multi.campus.clean.dao.GarbageCollectionDao">
	
	<select id="count" resultType="int">
		select count(*) from garbage_collection
	</select>
	
	<select id="getPage" resultType="garbageCollection">
		select collection_no, userid, cap, empty_date
		from (select row_number() over(order by collection_no desc) as seq, collection_no, userid, cap, empty_date from garbage_collection) 
		where seq between #{start} and #{end}
	</select>
	
	<select id="getUserCollection" resultType="garbageCollection">
		select collection_no, userid, cap, empty_date
		from (select row_number() over(order by collection_no desc) as seq, collection_no, userid, cap, empty_date from garbage_collection) 
		where seq between #{start} and #{end} and userid = #{userid}
	</select>
</mapper>